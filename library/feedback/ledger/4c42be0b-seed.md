# Ledger seed — dyad-steward (n=1) — Feedback

> **One file = one contributor's testimonials.** Append-only; never edited by another dyad.
> contributor = `sha256:4c42be0b…f000` (dyad-steward).

Each entry = a survived dyadic-cycle:

1. **Transient Correction Forgetting** — Agent repeatedly reverted to using legacy terminology because corrections only existed in the chat history. Fix: Exported the nomenclature rule directly to the `AGENT.md` persistent context block.

**Pattern:** Cost migrated to the validator as repeated corrections for the same error. By formalizing the feedback into a persistent document, the cost of correction is paid exactly once.
